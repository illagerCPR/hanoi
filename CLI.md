# 汉诺塔命令行模式说明书（CLI.md）

本说明书面向 **AI Agent** 与自动化脚本，描述命令行挑战模式的全部命令与约定。命令行模式只有**挑战模式**（由用户/Agent 自己操作通关），自动模式与推断模式仅存在于图形界面。

## 启动

```bash
python main.py --cli
```

- 输入输出均为 **UTF-8 编码**。
- 程序以交互式逐行读取标准输入（`stdin`），逐行输出到标准输出（`stdout`），每次输入一行命令，收到完整响应后再输入下一条。
- 非交互使用（管道/重定向/子进程）同样支持。逐行解析，无 ANSI 转义序列，便于文本解析。

## 命令总览

| 命令 | 作用 |
| --- | --- |
| `start <n>` | 开始新的挑战，n 为层数 |
| `move <a> <b>` | 移动盘子：从柱子 a 移到柱子 b |
| `hint` | 提示下一步建议移动 |
| `best` | 显示当前层数的理论最优步数 |
| `status` | 显示当前棋盘状态与步数 |
| `record` | 查看历史挑战记录 |
| `help` | 显示帮助 |
| `quit` / `exit` / `q` | 退出程序 |

命令不区分大小写，多余参数将被忽略（除 `move` 外的命令不接受参数）。

## 柱子与盘子命名

- 三根柱子编号为 **0 / 1 / 2**（0 为起始柱，2 为目标柱），也可用别名 **A / B / C**（A=0，B=1，C=2）。命令中混用两种写法均可。
- 盘子在棋盘上用数字标识：数字越大盘子越大（最大盘编号为层数）。
- 初始状态：所有盘子在柱子 0，从上到下为 1 到 n。

## 规则

- 一次只能移动一个盘子（每柱栈顶的盘子）。
- 大盘不能放在小盘之上。
- 目标：把全部 n 个盘子从柱子 0 移到柱子 2。
- 完成挑战后自动记录：时间戳、层数、移动步数、理论最佳步数（2^n − 1）、挑战时长。
- 层数范围：`start <n>` 接受 n 为 5~10；**通关 10 层后可挑战更高层数**（`help` 命令中会显示最新允许范围）。
- 通关 10 层前，程序不会以任何形式透露更高层数的信息。

## 命令详解

### start

```
> start 7
开始挑战：7 层（理论最优步数: 127）
```

- 合法层数范围见 `help`。超出范围时返回错误，例如：
  ```
  > start 4
  错误: 层数必须在 5 到 10 之间
  ```
- 重复 `start` 会丢弃当前未完成的对局，重新开始。

### move

```
> move 0 2
```

- 成功：输出完整棋盘（ASCII）与当前步数。
- 失败（返回以 `错误: ` 开头的消息，不消耗步数，棋盘不变）：
  - `错误: 用法: move <a> <b>` — 参数数量不对
  - `错误: 柱子编号必须是 0/1/2 或 A/B/C` — 非法柱子名
  - `错误: 源柱与目标柱相同` — a 与 b 相同
  - `错误: 源柱上没有盘子` — 源柱为空
  - `错误: 非法移动（大盘不能放在小盘上）` — 违反规则

### hint

返回一条建议移动 `建议下一步: <a> → <b>`。建议基于当前盘面实时计算，保证路径可行（玩家走过弯路时也会引导完成）。

### best

返回理论最优步数 `理论最优步数: <N>`。

### status

输出当前棋盘、步数、最优步数与用时。

### record

输出历史挑战记录表格（时间 / 层数 / 步数 / 最优 / 用时秒）。

### help

输出命令列表与当前允许的层数范围。

### quit

退出。

## 输出格式约定

- 所有错误消息以 `错误: ` 开头，可用此前缀识别异常。
- 棋盘格式示例（5 层开局）：

  ```
      0    1    2
  ===============
  | 1 ||   ||   |
  | 2 ||   ||   |
  | 3 ||   ||   |
  | 4 ||   ||   |
  | 5 ||   ||   |
  ---------------
  步数: 0 / 最优: 31
  ```

  每行三列分别对应柱子 0/1/2，从上到下为柱内盘子（底部为栈底）。

- 完成挑战时的输出：
  ```
  恭喜！你完成了汉诺塔挑战！
  层数: 5，步数: 31，理论最优: 31，用时: 8秒
  挑战记录已保存。
  ```

## 推荐策略（Agent 参考）

1. 先 `start <n>` 开始挑战。
2. 调用 `status` 确认盘面，或直接使用 `hint` 获得下一步。
3. 重复 `move` + `hint`（或自行规划最优解，步数上限 2^n − 1）直至通关。
4. 通关后用 `record` 查看记录确认保存。

## 示例会话（5 层，使用 hint 指引）

```
> start 5
开始挑战：5 层（理论最优步数: 31）
（棋盘略）
> hint
建议下一步: 0 → 2
> move 0 2
（棋盘更新，步数: 1）
> hint
建议下一步: 0 → 1
> move 0 1
（棋盘更新，步数: 2）
...
> move 0 2
（棋盘更新，步数: 31）
恭喜！你完成了汉诺塔挑战！
层数: 5，步数: 31，理论最优: 31，用时: 42秒
挑战记录已保存。
```

## 数据文件

- `data/records.json`：挑战记录列表。
- `data/progress.json`：通关进度（已通过的最高层数）。
- 两个文件在首次运行时自动创建；GUI 与 CLI 共享同一份数据。
- 可通过环境变量 `HANOI_DATA_DIR` 覆盖数据目录（便于测试隔离）。

## 常见问题

- 输入被一次写入多行（管道）：程序逐行处理，等价于逐条输入。
- 编码：请以 UTF-8 读写 stdin/stdout。
- `start` 后棋盘未变化：检查上一条 `move` 是否返回了 `错误:` 消息。
